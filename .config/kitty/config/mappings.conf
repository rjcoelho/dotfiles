# Set the value of kitty_mod modifier for all mappings (default is ctrl+shift).
kitty_mod super

# Remove all default mappings.
#clear_all_shortcuts yes

# Clipboard operations.
map kitty_mod+c copy_to_clipboard
map kitty_mod+v paste_from_clipboard
map kitty_mod+s paste_from_selection

# Change font sizes.
map kitty_mod+equal change_font_size all +2.0
map kitty_mod+minus change_font_size all -2.0
map kitty_mod+0 change_font_size all 0

# Utilities.
map kitty_mod+i kitten unicode_input
map kitty_mod+k clear_terminal reset active # default is kitty_mod+delete